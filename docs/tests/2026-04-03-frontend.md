# Frontend QA Evidence — 2026-04-03 / 2026-04-04

## 2026-04-04 — Postcode Flow via Cloudflare Tunnel
- **Tunnel**: `https://parade-displays-saturn-courier.trycloudflare.com` (frontend) + `https://philip-gotta-bibliographic-edge.trycloudflare.com` (Strapi direct)
- **Script**: `QA_BASE_URL=$BASE QA_STRAPI_BASE_URL=$STRAPI QA_ACCOUNT=test2@greenhub.co.uk QA_PASSWORD=TestPass123! QA_ADMIN_TOKEN=... node scripts/gui-e2e.mjs`
- **Order**: `ORD-1775295285051-2` (wallet checkout, Berry Kush 3.5g)
- **Wallet after checkout**: £315.50 via `/api/strapi/api/wallet/balance`

### Evidence
| Type | Path |
| --- | --- |
| Screenshots | `docs/tests/evidence/2026-04-04/gui-postcode/{wallet.png, checkout-postcode.png, order-success.png, orders-list.png, order-detail.png, notifications.png}` |
| HAR | `docs/tests/evidence/2026-04-04/gui-postcode/gui-postcode.har` (all `/api/strapi/*` → 2xx) |
| Locker curl log | `docs/tests/evidence/2026-04-04/gui-postcode/locker-curl.log` (cf-ray: `9e6f540e6a11eb1c`, HTTP/2 200) |

### Locker Assignment
```bash
curl -X PUT "https://philip-gotta-bibliographic-edge.trycloudflare.com/api/orders/ORD-1775295285051-2/locker" \
  -H "Content-Type: application/json" \
  -H "x-admin-token: 08e2cbc8c4479dd0a6745220cebbaf2d8c26f0026c8df1ebc9cce7316ea88a47" \
  -d '{"lockerAddress":"123 Locker St","lockerAccessCode":"CODE123","lockerNotes":"24/7 access"}'
# → 200 OK, lockerAssignedAt=2026-04-04T09:36:22Z
```

Notifications頁顯示「Locker ready」提醒（見 `notifications.png`），對應 Strapi `lockerAssignedAt` 與 email/message 記錄。

## 2026-04-04 — Wallet Top-up (NowPayments QA)
- **Tunnel**: `https://brass-identical-stunning-investigator.trycloudflare.com` (frontend, cf-ray `9e71008b9ef2b13e-MAN`) + `https://individuals-litigation-electronic-wrapping.trycloudflare.com` (Strapi, cf-ray `9e7100e88cceeb14-MAN`).
- **Flow**: Playwright 登入 QA 帳 → `/wallet` 基準截圖 → `/wallet/topup` 按 “Get payment link” → `Invoice TU-1775312625587-2` 顯示 NowPayments 連結。
- **Top-up**: `topupId=6`, `orderCode=TU-1775312625587-2`, `invoiceUrl=https://nowpayments.io/payment/?iid=5841611210`, amount £100 (TRC20) + bonus £5。
- **IPN**: `curl -X POST $STRAPI_TUNNEL/api/nowpayments/ipn --data @ipn-payload.json`（HMAC-SHA512 排序後簽名，payload/response 於 evidence 目錄）。
- **Wallet**: before £140.50 → after £245.50；`wallet-transactions.json` 顯示 `topup-6` + `bonus-6`。

### Evidence
| Type | Path |
| --- | --- |
| Screenshots | `docs/tests/evidence/2026-04-04/topup/{wallet-before.png, topup-invoice.png, wallet-after.png}` |
| JSON / Logs | `docs/tests/evidence/2026-04-04/topup/{topup-intent.json, ipn-payload.json, ipn-response.json, wallet-transactions.json, sqlite-topups.txt, strapi-ipn.log}` |
| Command | `docs/tests/evidence/2026-04-04/topup/ipn-payload.json`（供 `curl -X POST .../api/nowpayments/ipn --data @file` 使用） |

## 2026-04-04 — Wallet Withdraw QA
- **Tunnels**: `https://paul-decent-column-acknowledge.trycloudflare.com` (Next production via `next start --hostname 0.0.0.0 --port 3000`) + `https://clip-inquiries-academic-taste.trycloudflare.com` (Strapi prod build).
- **Script**: `QA_BASE_URL=$BASE QA_STRAPI_BASE_URL=$STRAPI QA_ACCOUNT=test2@greenhub.co.uk QA_PASSWORD=TestPass123! QA_ADMIN_TOKEN=... node scripts/gui-withdraw.mjs` (Playwright default 20 s step timeout / 60 s nav).
- **Wallet**: before run £189 via `/api/strapi/api/wallet/balance` (cf-ray `9e720c458f50b33a-MAN`).
- **Flows**:
  - Bank transfer £25 → reference `mcnuod582uscvlh4fk6syrsg` / Strapi ID **59** / cf-ray `9e720b6998beb33a-MAN`.
  - USDT £30 (TRC20) → reference `khkzezppegnorn3hnwxb8k17` / Strapi ID **60** / cf-ray `9e720b7b3f48b33a-MAN`.
- **Page load**: `/wallet/withdraw` GET cf-ray `9e720b5c2d23b33a-MAN`; production build removes prior RSC fetch failures. Remaining console 404s are harmless prefetches for `/rewards`, `/lockers`, `/press`, `/privacy` (no impact on flow).
- **Strapi evidence**: `strapi-withdrawals.json` (filtered to QA user, populated with related wallet transactions). Strapi Admin UI currently blocks Cloudflare host with `403 server.allowedHosts` (see `strapi-admin-host-error.txt`), so direct admin screenshot will remain unavailable until host allowlist is updated upstream.
- **History**: `history.png` shows both pending requests with success cards, `wallet.png` captures balance banner pre-withdraw.

### Evidence
| Type | Path |
| --- | --- |
| Screenshots | `docs/tests/evidence/2026-04-04/withdraw/{wallet.png, bank-step1.png, bank-step2.png, bank-success.png, usdt-step1.png, usdt-step2.png, usdt-success.png, history.png}` |
| HAR / JSON | `docs/tests/evidence/2026-04-04/withdraw/withdraw.har`, `wallet-balance.json`, `strapi-withdrawals.json`, `strapi-admin-host-error.txt` |
| Notes | cf-ray traces above logged per entry; Playwright console clean aside from expected Next prefetch 404s. |

---

## 2026-04-05 — Account Settings QA (ACC-ACCOUNT-SETTINGS)

### Scope
- `/account` profile update (phone, telegram, preferred locker, marketing opt-in)
- `/account/security` email change + password change + revert
- Backend: Customer schema + Account API + fullName backfill

### Backend Changes
- Customer schema: +`fullName`, +`telegramHandle`, +`preferredLocker`, +`marketingOptIn`
- Account controller: `GET /api/account/profile` (strips passwordHash, backfills fullName from name)
- Account controller: `PUT /api/account/profile` (phone/telegram/locker/marketing)
- Order controller: restored `findOneForCustomer` handler (missing after REV-02c merge)
- Lifecycle: `fullName ↔ name` sync on create/update
- `tsc --noEmit`: 0 errors

### Frontend Changes
- `PUT /api/account/profile` proxy → Strapi `/api/account/profile`
- `POST /api/account/security/change-email` → fixed `resolveServerBase` (was using relative path)
- `POST /api/account/security/change-password` → fixed `resolveServerBase`
- `pnpm lint`: 0 errors, `pnpm test`: 5/5 passed

### Playwright E2E Results

| Step | Action | Result |
| --- | --- | --- |
| 1 | Login (test2@greenhub.co.uk) | ✅ |
| 2 | Set phone (+447700900222) | ✅ Profile updated |
| 3 | Clear phone | ✅ Profile updated |
| 4 | Restore phone | ✅ Profile updated |
| 5 | Change email (temp) | ✅ Email updated |
| 6 | Change password (temp) | ✅ Password updated |
| 7 | Revert password | ✅ Password updated |
| 8 | Account screenshot | ✅ |
| 9 | Notifications screenshot | ✅ |
| 10 | Revert email | ✅ Email updated |
| 11 | Restore original phone | ✅ Profile updated |

### Evidence

| Type | Path |
| --- | --- |
| Screenshots | `docs/tests/evidence/2026-04-04/account-settings/{account-phone-updated.png, account-email-updated.png, security-email-success.png, security-password-change.png, notifications-security.png}` |
| API JSON | `docs/tests/evidence/2026-04-04/account-settings/{account-profile.json, wallet-balance.json}` |
| Email log | `docs/tests/evidence/2026-04-04/account-settings/latest-email.txt` |

### SQL Migration Notes (for PostgreSQL deploy)

```sql
-- Customer table: add new columns
ALTER TABLE customers ADD COLUMN full_name VARCHAR(255) DEFAULT NULL;
ALTER TABLE customers ADD COLUMN telegram_handle VARCHAR(255) DEFAULT NULL;
ALTER TABLE customers ADD COLUMN preferred_locker VARCHAR(255) DEFAULT NULL;
ALTER TABLE customers ADD COLUMN marketing_opt_in BOOLEAN DEFAULT FALSE;

-- Backfill fullName from name
UPDATE customers SET full_name = name WHERE full_name IS NULL AND name IS NOT NULL;
```
